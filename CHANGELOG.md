# 変更履歴

リリース時、このファイルから該当バージョンの節が抜き出され、
GitHub Release の本文に「変更点」として差し込まれる。
見出しは `## vX.Y.Z` の形式にすること（この形で検索している）。

## v1.2.0

### 追加

- **生年月日と性別の登録**：初回起動時に登録をお願いする形にしました。あとから変更もできます。
  献血予約サイトの空き状況ページはこの2つを条件として受け取る作りになっており、
  これまでは固定のダミー値（2006年1月1日生まれの男性）を送っていました。
  **架空の値を送るのは不誠実である**ため、ご自身の値を登録していただく形に改めています
- この情報は**空き状況の照会にのみ使用し、作者を含む第三者へ送信されることはありません**。
  保存先はお使いのPC内（`%APPDATA%\KenketsuAvailability\`）だけです
- 作者側の設定で、特定のバージョンの利用を停止できるようにしました。
  献血予約サイトの構成変更などで正しく動かなくなった版を止めるための仕組みです。
  該当した場合は検索画面を出さず、最新版のダウンロードを案内します

### ドキュメントの修正

- ラブラッドの利用規約と `robots.txt` を確認し、**自動的なアクセスを禁止する条項が無い**ことを
  根拠つきで記載しました（ただし、禁止されていないことと想定された使い方であることは別です）
- 「遠隔での停止」という説明が、利用者のPCを遠隔操作できるかのような誤解を招くため、
  **アプリが起動時に設定ファイルを読みにいくだけである**ことを説明する形に改めました
- ソースコードを改変して使う場合の免責を明記しました

### 旧バージョンの配布終了

v1.0.0 および v1.1.0 は配布を終了しました（ダミー値を送り続けるため）。
リリースの履歴は残していますが、exe のダウンロードはできません。

## v1.1.0

### 追加

- **日付横断モード**：1つの献血ルームについて、複数の日の空き状況をまとめて検索できるようになりました。
  「特定のルームに行きたいが、いつ空いているか知りたい」という探し方ができます
  （従来の「1日 × 複数ルーム」は**ルーム横断モード**として残っています）
- 対象日は**カレンダーから複数選択**できます。1回の検索で選べるのは 7日 までです
- アプリアイコンを追加しました

### 変更

- 献血ルームの取扱種別を**全血400・血漿・血小板それぞれのフラグ**に分けました。
  全血のみ・血漿のみを扱うルームがあるため、これまでより正確に
  「取扱なし」と「空きなし」を出し分けられます
- 日付横断モードの献血ルーム選択を、**モーダル＋インクリメンタル検索**にしました
  （ルーム名・都道府県・地方で絞り込めます）

### v1.0.0 をお使いの方へ

献血ルームのマスタ形式が変わったため、v1.0.0 のままでは
**血小板の「取扱なし」が「空きなし」と表示されます**。
表示上の違いだけで検索自体は動きますが、更新をおすすめします。

## v1.0.0

初回リリース。

- 1つの日について、複数の献血ルームの予約空き状況を横断して検索
- 全血400・血漿・血小板の空き時間を表示
- 地方・都道府県単位でのまとめ選択と、「条件セット」の保存
- 日本赤十字社のサイトへの負荷を抑えるレートリミット
